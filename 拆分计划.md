# Bazaar_Lens.py 拆分计划

## 已完成 ✅

1. **config.py** - 配置管理模块
   - ConfigManager 类
   - 配置文件读写功能

2. **logger.py** - 日志管理模块
   - get_log_file_path() 函数
   - setup_logging() 函数
   - hide_console() / show_console() 函数
   - is_packaged_environment() 函数

3. **ocr.py** - OCR功能模块
   - ocr_task() 函数
   - direct_ocr() 函数

4. **system/admin.py** - 管理员权限模块
   - is_admin() 函数
   - run_as_admin() 函数

## 待完成 📋

### 1. UI组件模块 (ui/)
- [ ] **ui/__init__.py** - 模块初始化
- [ ] **ui/components.py** - GUI组件
  - IconFrame 类 (第211行)
  - ScrollableFrame 类 (第455行)

### 2. 数据管理模块 (data/)
- [ ] **data/__init__.py** - 模块初始化
- [ ] **data/loader.py** - 数据加载函数
  - load_monster_data()
  - load_event_data()
  - load_items_data()
- [ ] **data/matcher.py** - 匹配逻辑
  - 文本匹配相关函数
  - 相似度计算

### 3. 游戏相关模块 (game/)
- [ ] **game/__init__.py** - 模块初始化
- [ ] **game/monitor.py** - 游戏日志监控
  - 日志监控线程
  - InstanceId 映射
- [ ] **game/position.py** - 位置配置
  - load_position_config()
  - calculate_relative_offset()

### 4. 系统功能模块 (system/)
- [ ] **system/tray.py** - 系统托盘
  - SystemTray 类 (第6141行)
- [ ] **system/update.py** - 更新检查
  - 自动更新相关功能

### 5. 主程序重构
- [ ] **Bazaar_Lens.py** - 主程序
  - 保留 BazaarHelper 主类 (第616行)
  - 更新所有导入语句
  - 更新类和方法调用

## 拆分步骤

1. 先完成所有独立模块的拆分
2. 更新主程序中的导入语句
3. 测试每个模块是否正常工作
4. 修复可能的导入错误
5. 最终测试整个程序

## 注意事项

- 保持原有功能不变
- 确保所有导入路径正确
- 注意循环导入问题
- 保持代码风格一致（4空格缩进）
