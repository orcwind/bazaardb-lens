# 将 Tesseract 打包到安装程序的说明

## 📥 第一步：下载 Tesseract（最新可用版本）

### 下载地址
- **GitHub 发布页面**: https://github.com/UB-Mannheim/tesseract/releases
- **SourceForge**: https://sourceforge.net/projects/tesseract-ocr.mirror/files/

### 下载文件
下载 **Windows 64位安装程序**（当前最新版本）：
- **推荐版本**：`tesseract-ocr-w64-setup-5.4.0.20240606.exe`（约 47.9 MB）
- 或选择其他可用的 5.x 版本

**注意**：
- 5.5.2 目前只有源代码，尚未提供 Windows 安装程序
- 5.4.0 是当前最新的稳定 Windows 安装版本
- 5.4.0 已经足够使用，性能良好

## 📦 第二步：准备 Tesseract 文件

### 方法1：从已安装的 Tesseract 复制（推荐）

1. **安装 Tesseract**：
   - 运行下载的安装程序
   - 安装到默认位置：`C:\Program Files\Tesseract-OCR`
   - **重要**：安装时选择安装以下语言包：
     - ✅ English (eng)
     - ✅ Chinese Simplified (chi_sim) - **必须**
     - ✅ Orientation and script detection (osd)

2. **复制 Tesseract 目录**：
   ```bash
   # 在项目根目录下创建 Tesseract-OCR 文件夹
   # 然后将整个安装目录复制过来
   xcopy "C:\Program Files\Tesseract-OCR\*" "D:\PythonProject\BazaarInfo\bazaardb-desktop\Tesseract-OCR\" /E /I /Y
   ```
   
   或者手动操作：
   - 在项目根目录创建 `Tesseract-OCR` 文件夹
   - 将 `C:\Program Files\Tesseract-OCR\` 下的所有文件和文件夹复制到 `Tesseract-OCR\`

3. **验证文件结构**：
   确保以下文件存在：
   ```
   Tesseract-OCR/
   ├── tesseract.exe          (主程序)
   ├── tessdata/              (语言包目录)
   │   ├── chi_sim.traineddata  (简体中文，必须)
   │   ├── eng.traineddata      (英文)
   │   └── osd.traineddata      (方向检测)
   └── [其他 DLL 和配置文件]
   ```

### 方法2：从安装程序提取（高级）

如果需要自动化，可以使用 7-Zip 或其他工具提取安装程序内容。

## 🔧 第三步：使用新的安装脚本

### 使用便携版安装脚本
使用 `Bazaar_Lens_Installer_Portable.iss` 而不是原来的 `Bazaar_Lens_Installer.iss`

### 编译安装程序
1. 打开 Inno Setup Compiler
2. 打开 `Bazaar_Lens_Installer_Portable.iss`
3. 点击 Build → Compile（或按 Ctrl+F9）
4. 等待编译完成

## ✅ 优势

### 便携版方案的优势：
1. ✅ **无需管理员权限**：不需要安装到 Program Files
2. ✅ **所有文件在一起**：便于管理和卸载
3. ✅ **更简单的安装流程**：用户只需运行一个安装程序
4. ✅ **自动配置路径**：程序会自动找到应用目录下的 Tesseract
5. ✅ **卸载更干净**：卸载时一起删除所有文件

### 文件大小
- Tesseract 文件约 50-80 MB
- 语言包（chi_sim）约 43 MB
- 总安装包大小约 150-200 MB

## 📝 注意事项

1. **许可证**：Tesseract 使用 Apache 2.0 许可证，可以自由分发
2. **语言包**：确保包含 `chi_sim.traineddata`（简体中文）
3. **路径配置**：程序会自动检测应用目录下的 Tesseract，无需手动配置
4. **测试**：安装后务必测试 OCR 功能是否正常

## 🐛 故障排除

### 问题1：找不到 Tesseract
- 检查 `{app}\Tesseract-OCR\tesseract.exe` 是否存在
- 检查程序配置中的路径是否正确

### 问题2：OCR 识别失败
- 检查 `{app}\Tesseract-OCR\tessdata\chi_sim.traineddata` 是否存在
- 检查文件是否完整（chi_sim.traineddata 应该约 43 MB）

### 问题3：安装包太大
- 可以只包含必要的语言包（chi_sim, eng, osd）
- 删除不需要的 DLL 文件（如果确定不需要）

